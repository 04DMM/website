<%- include('./_partial/header.ejs', { title: 'Select a World' }); %>
<%- include('./_components/frame-top.ejs'); %>
<%- include('./_components/content-start.ejs'); %>
<%- include('./_components/title-box.ejs', { title: 'Play 04 Tournament', links: [ { text: 'Detail Select', href: '/detail' } ] }); %>


<center>
<br>

<center><table width=500 bgcolor=black cellpadding=4 border=0><tr><td class=e>
<center><b>Select a Tournament Realm</b><br><br>
<table>
  <tr valign="top">
    <% for (let i = 0; i < membersRegions.length; i++) { let regionWorlds = worlds.filter(x => x.region == membersRegions[i] && x.members); %>
      <% if ((i % 3) == 0) { %>
        <td align=center width=165>
        <table>
      <% } %>
          <tr><td colspan=2>&nbsp;&nbsp;<img align=absmiddle src=/img/title/sword-new.jpg width=50 height=50 border=0> <%= membersRegions[i] %></td></tr>
          <% for (let j = 0; j < regionWorlds.length; j++) { %>
            <tr><td valign=center><a href="/client?world=<%= regionWorlds[j].id %>&detail=<%= detail %>&method=<%= method %>" oncontextmenu=return(false) target="_parent" class=c>World <%= regionWorlds[j].id %></a></td><td align=right>LIVE</td></tr>
          <% } %>
          <% if (i + 1 < membersRegions.length) { %>
            <tr><td colspan=2>&nbsp;</td></tr>
          <% } %>
      <% if (((i + 1) % 3) == 0) { %>
        </table>
        </td>
      <% } %>
      <% if (membersRegions.length % 3 != 0 && i == membersRegions.length - 1) { %>
        </table>
        </td>
      <% } %>
    <% } %>
  </tr>
</table><br>
</td></tr></table><br></center>
</td></tr></table>

<%- include('./_partial/footer.ejs'); %>
